services:
  - type: web
    name: free-n8n-instance
    runtime: docker
    plan: free
    region: oregon
    envVars:
      - key: N8N_PORT
        value: 10000
      - key: N8N_METRICS
        value: "false"
